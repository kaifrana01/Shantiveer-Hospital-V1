# Core Framework
Django>=5.0,<6.0
djangorestframework>=3.14.0

# Image handling
Pillow>=10.0.0

# PostgreSQL (optional — only needed if using PostgreSQL instead of MySQL)
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# MySQL support (Hostinger VPS / local dev only — NOT required on Vercel/PostgreSQL deployments)
# Install manually: pip install mysqlclient>=2.2.0
# mysqlclient>=2.2.0

# pymysql: pure-Python MySQL driver, used as MySQLdb shim on VPS (set USE_MYSQL=true).
# Safe to install everywhere — no compilation required.
pymysql>=1.1.0

# Date/time utilities (used for DOB-based age calculation in uhid/views.py)
python-dateutil>=2.8.2

# Static files serving
whitenoise>=6.6.0

# Production WSGI server
gunicorn>=21.2.0

# Environment variable loading
python-dotenv>=1.0.0



# Audit trail / history tracking for high-priority financial & clinical models
django-simple-history>=3.5.0

# PDF generation (used for income/USG/lab daybook exports)
xhtml2pdf>=0.2.11
reportlab>=4.0.0

# Cloud media storage — Cloudinary (free 25 GB, no credit card needed).
# Required when CLOUDINARY_CLOUD_NAME env var is set (Vercel deployments).
# django-cloudinary-storage provides the Django storage backend.
# cloudinary is the core SDK it depends on.
django-cloudinary-storage>=0.3.0
cloudinary>=1.40.0
